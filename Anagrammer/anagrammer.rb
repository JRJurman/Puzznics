#anagrammer.rb by Jesse Jurman
#anagram solver

# takes in a dictionary file and a block to check lines with
# returns a list of valid words, reverse ordered by size (largest first)
def loadDict(dictFile, func)
end

# takes in a filename and a string of letters (the anagram)
# returns the largest valid word from the anagram
def solve(dictFile, anagram)
end
